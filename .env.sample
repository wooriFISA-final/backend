DB_HOST=localhost
DB_PORT=3306
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=your_dbname

# JWT settings
# 난수의 기반이 될 값 아무거나
JWT_SECRET_KEY=a-zA-Z!@^
JWT_ALGORITHM=HS256

# host
BACKEND_URI=localhost